# Application
PORT=4000
NODE_ENV=development

# Database (PostgreSQL via Prisma)
DATABASE_URL="postgresql://postgres:u%40123@localhost:5432/curify?schema=public"

# JWT
JWT_SECRET='337c1b28efba29935b7c06fcd7dd8bfc666e0fe5784bbc82a388a23856f083db'
JWT_EXPIRES_IN=7d

# OpenAI
OPENAI_API_KEY=sk-proj-*********


# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

#google place api key  
GOOGLE_PLACE_API_KEY=

# Two separate YouTube keys → separate per-key daily quotas.
YOUTUBE_API_KEY_1=   # leads pipeline (admin/leads)
YOUTUBE_API_KEY_2=   # hospital scraper

# Daily auto-runs at 02:00 IST (catch up on restart if the box was off):
#   leads cron  → LEADS_CRON_DISABLED=1 to disable
#   scrape cron → SCRAPE_CRON_DISABLED=1 to disable
